# IOS Markdown App for preview and editor


## 需求背景
我是一名WEB前端开发工程师，我自己有Mac 和 IOS 设备 

我想拥有一款属于自行的 markdown 文档 预览和编辑 App

我对 IOS App 开发一无所知

## MVP 需求清单🧾

### MD 文档预览
1. App 支持从本地文件系统选择 文档进行预览
2. App 支持通过 其他App中的 MD文档分享到 本App 中打开预览

### MD文档编辑
1. 外部导入的MD文档支持切换到 编辑 模式
2. 支持新增 MD文档（存储在系统文件管理系统，也可以是iCloud，支持同步）
3. App内容的文档支持目录管理，可以支持无限制级目录

### 导入
1. 外部App 分享到此App时有一个中间步骤，确认放在哪一个目录，默认放在根目录
2. 导入之后默认进入 预览模式

### 模式
1. 预览和编辑模式的切换可以在右上角有一个 trigger button group


## 技术栈要求

1. 我希望使用原生 swift 技术栈开发（我很想使用 液态玻璃效果）
2. 我希望支持 light/dark 主题适配
3. 预览模式我很想使用 webview 的形式，使用 github markdown 主题（我不确定是否可行）
4. 编辑模式我希望使用 原生，但是我有点担心效果

## 其他说明
目前整个项目目录都是空的，我连需要准备什么 编辑器我也不知懂，例如xcode 之类的，一切从零开始

 验收通过了；
  我接下来继续要做的事情 Todo List：
  1. 我把App 名称 改为 "Markdown Lite"，寓意轻量化的markdown 编辑预览（如果针对这个寓意你有好的想法也可以和我讨论）
  2. 在预览模式的时候，如果文档中有外部链接，点击的以后会直接覆盖当前的review进行连接内容预览（但是顶部按钮和 文档切换按钮还在，体验有点糟糕，我希望的能单独弹出一个Modal预览或者使用本地默认浏览器打开）
  3. 目前即时预览的保存导入功能 和 其他app 分享进来的文档 保存
  到app功能都有一个小问题，选择目录时如果没有合适的目录不能自主新建一个目录，我希望能支持这个功能
  4. 在预览状态的时候，右上角增加一个 分享按钮 share，点击按钮弹出 三个选择模式：
     4.1. 截图分享，把整个文档的生成一个长截图 分享出去
     4.2. 分享源文件 xxx.md
     4.3. 分享源内容 文档源内容
  5. 在编辑状态 右上角的按钮变成一个 AI辅助编辑 按钮，先占位，我还没想好怎么做

你需要这样做：
1. 把现有的 work-plan 移除 S8～S9，然后归档提交
2. 新增一个 work-plan，加入 上面的 1～5， S8～S9 的内容放在最后 作为 S6～S7


## TODO list
- Diff 改动可视化
- 多语言 prompt
- 编程语言支持
- API 配置页面提示 DeepSeek MiniMax Claude ChatGPT BigModel
- AI placeholder
- 


## 新功能：提问确认/二次生成/

1. 目前的AI辅助生成功能，都是一次性的，模型返回完成之后 用户只能预览结果，如果不满意的话不能二次通过输入告诉Agent进行调整或者重新生成；所以生成完成之后要继续显示输入和按钮；
2. 开始生成提交请求API之后，Agent目前是立即就开始生成了；这其实是有问题，用户的诉求如果描述含糊不清的时候，生成出来的结果肯定是不好的，所以针对诉求不清楚的情况，应该支持反问，挖掘诉求；
   1. 技术支持：使用 现在 Agent API 都支持的function tools；定一个function tool，作用就是让模型能反问挖掘诉求；
   2. tool 如参：问题、回答要求（就支持 单选/多选/文字输入 三种，选择类型需提供可选项，推荐项）；用户回答之后发给Agent API 继续生成；
   3. tool被条调用的时候 App 拿到之后要可视化渲染出来 供用户 回答交互；
   4. Agent API 返回 tool 调用的内容是不能渲染到 内容区域的；

以上功能需要考虑好友好的交互体验，尤其在手机端这种小屏幕可操作性要求高，iPad还好；
不要那种 长长的扁扁的按钮；
不要那种 边距贴很近的文案；
